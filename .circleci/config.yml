version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Install System Dependencies
          command: bash install_system_dependencies.sh
